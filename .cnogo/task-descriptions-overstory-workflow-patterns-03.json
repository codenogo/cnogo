[
  {
    "name": "Create watchdog.py stale task detector",
    "description": "# Implement: Create watchdog.py stale task detector\n\nCreate new module scripts/memory/watchdog.py (stdlib only). Implement: (1) check_stale_tasks(root, stale_minutes=10) -> list[dict]: read worktree-session.json, find tasks with status='executing' whose timestamp is older than stale_minutes. Return list of {task_index, name, branch, minutes_stale, memory_id}. (2) check_stale_issues(root, stale_days=30) -> list[dict]: query memory DB for issues that are open, unassigned, and created_at older than stale_days. Return list of {id, title, days_stale}. (3) record_stale_event(root, stale_info, actor='watchdog'): record a 'stale_detected' event in memory DB for each stale task/issue. (4) run_all_checks(root, config) -> dict: run both checks using thresholds from WORKFLOW.json agentTeams.staleIndicatorMinutes and freshness.contextMaxAgeDays. Return {stale_tasks: [...], stale_issues: [...], checked_at: timestamp}. In __init__.py: add watchdog exports to __all__ with lazy imports (check_stale_tasks, check_stale_issues, run_watchdog_checks).\n\n**Files (ONLY touch these):**\n`scripts/memory/watchdog.py`, `scripts/memory/__init__.py`\n\n**Verify (must ALL pass):**\n- `python3 -m py_compile scripts/memory/watchdog.py`\n- `python3 -c \"from scripts.memory.watchdog import check_stale_tasks, check_stale_issues; print('OK')\"`\n\n**Memory:** `cn-12vmyu0.1`\n- Claim: `python3 scripts/workflow_memory.py claim cn-12vmyu0.1 --actor implementer`\n- Close: `python3 scripts/workflow_memory.py close cn-12vmyu0.1 --reason completed`\n- Context: `python3 scripts/workflow_memory.py show cn-12vmyu0.1`\n- History: `python3 scripts/workflow_memory.py history cn-12vmyu0.1`\n- Checkpoint: `python3 scripts/workflow_memory.py checkpoint`\n\n**On failure:** read history, summarize the last error, fix, and retry.\n**Retry loop:** after each failed verify, run the history command before next attempt.\nAfter 2 failures, message the team lead.\n**If blocked:** do NOT close memory. Message the team lead.\n",
    "memoryId": "cn-12vmyu0.1",
    "files": [
      "scripts/memory/watchdog.py",
      "scripts/memory/__init__.py"
    ],
    "verify": [
      "python3 -m py_compile scripts/memory/watchdog.py",
      "python3 -c \"from scripts.memory.watchdog import check_stale_tasks, check_stale_issues; print('OK')\""
    ],
    "blockedBy": [],
    "skipped": false
  },
  {
    "name": "Add doctor subcommand to workflow_checks",
    "description": "# Implement: Add doctor subcommand to workflow_checks\n\nAdd 'doctor' subcommand to workflow_checks_core.py. In main(): add parser for 'doctor' with optional --json flag. Implement _cmd_doctor(root, wf, json_output=False) -> int that runs 5 checks sequentially: (1) Workflow validation: subprocess.run(['python3', 'scripts/workflow_validate.py'], capture_output) \u2014 pass/fail based on exit code. (2) Memory DB integrity: open .cnogo/memory.db, run 'PRAGMA integrity_check', pass if result is 'ok'. Skip if DB doesn't exist. (3) Orphaned worktrees: run 'git worktree list --porcelain', compare against worktree-session.json \u2014 warn on worktrees not tracked by any session. (4) Stale issues: import watchdog.check_stale_issues(), warn if any found. (5) Hook config sanity: check .claude/settings.json exists, verify hook script paths in settings point to existing files. Print pass/warn/fail per check. Return 0 if all pass/warn, 1 if any fail. JSON mode outputs {checks: [{name, status, details}]}.\n\n**Files (ONLY touch these):**\n`scripts/workflow_checks_core.py`\n\n**Verify (must ALL pass):**\n- `python3 -m py_compile scripts/workflow_checks_core.py`\n- `python3 scripts/workflow_checks.py doctor --help`\n\n**Memory:** `cn-12vmyu0.2`\n- Claim: `python3 scripts/workflow_memory.py claim cn-12vmyu0.2 --actor implementer`\n- Close: `python3 scripts/workflow_memory.py close cn-12vmyu0.2 --reason completed`\n- Context: `python3 scripts/workflow_memory.py show cn-12vmyu0.2`\n- History: `python3 scripts/workflow_memory.py history cn-12vmyu0.2`\n- Checkpoint: `python3 scripts/workflow_memory.py checkpoint`\n\n**On failure:** read history, summarize the last error, fix, and retry.\n**Retry loop:** after each failed verify, run the history command before next attempt.\nAfter 2 failures, message the team lead.\n**If blocked:** do NOT close memory. Message the team lead.\n",
    "memoryId": "cn-12vmyu0.2",
    "files": [
      "scripts/workflow_checks_core.py"
    ],
    "verify": [
      "python3 -m py_compile scripts/workflow_checks_core.py",
      "python3 scripts/workflow_checks.py doctor --help"
    ],
    "blockedBy": [
      0
    ],
    "skipped": false
  },
  {
    "name": "Create /doctor slash command",
    "description": "# Implement: Create /doctor slash command\n\nCreate .claude/commands/doctor.md slash command. Content: run `python3 scripts/workflow_checks.py doctor` and report results. Include guidance on interpreting pass/warn/fail. Mention that --json flag is available for programmatic consumption. Reference the 5 checks (workflow validation, DB integrity, orphaned worktrees, stale issues, hook config) so users know what's being checked.\n\n**Files (ONLY touch these):**\n`.claude/commands/doctor.md`\n\n**Verify (must ALL pass):**\n- `test -f .claude/commands/doctor.md`\n\n**Memory:** `cn-12vmyu0.3`\n- Claim: `python3 scripts/workflow_memory.py claim cn-12vmyu0.3 --actor implementer`\n- Close: `python3 scripts/workflow_memory.py close cn-12vmyu0.3 --reason completed`\n- Context: `python3 scripts/workflow_memory.py show cn-12vmyu0.3`\n- History: `python3 scripts/workflow_memory.py history cn-12vmyu0.3`\n- Checkpoint: `python3 scripts/workflow_memory.py checkpoint`\n\n**On failure:** read history, summarize the last error, fix, and retry.\n**Retry loop:** after each failed verify, run the history command before next attempt.\nAfter 2 failures, message the team lead.\n**If blocked:** do NOT close memory. Message the team lead.\n",
    "memoryId": "cn-12vmyu0.3",
    "files": [
      ".claude/commands/doctor.md"
    ],
    "verify": [
      "test -f .claude/commands/doctor.md"
    ],
    "blockedBy": [
      1
    ],
    "skipped": false
  }
]